ALGORITHME NomAlgo
constante:
	
variable:
	bonjour, x, y, z: entier
	a, b : chaine
DEBUT

	bonjour <-- 8/2
	x <-- 38
	y <-- x×2
	a <-- "VERGE DE CHEVRE"
	
	lire( x )
	
	ecrire ( "x+y:"©(x+y) )
	ecrire ( x )
	ecrire ( y )
	ecrire ( bonjour )
	
	tq x < 10 faire
		ecrire ( "Truc" )
		x <-- x+1;
	ftq
	
	x <-- 0
	
	tq x <= 2 faire
		si x = 4 alors
			ecrire ( "Test" )
		sinon
			ecrire ( "test" )
		fsi
		x <-- x+1
	ftq
	
	
	si x=38 alors
		ecrire ( "etape 1" )
		x <-- 10 
		 
		si y=2 alors
			ecrire ( "etape 2" )
		fsi
		
		ecrire ( "etape 3" )
		
		si y=2 alors
			ecrire ( "etape 4" )
		sinon
			ecrire ( "etape 5" )
		fsi
		
	fsi
	
	si y=7 alors
		ecrire ( "etape 6" )
	sinon
		ecrire ( "etape 7" )
	fsi
	
	ecrire ( "etape 8" )

FIN
